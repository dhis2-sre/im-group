apiVersion: v1
kind: Service
metadata:
  name: {{ include "im-deployable-namespace.fullname" . }}
  labels:
    {{- include "im-deployable-namespace.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "im-deployable-namespace.selectorLabels" . | nindent 4 }}
