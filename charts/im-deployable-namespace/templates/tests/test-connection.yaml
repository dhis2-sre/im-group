apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "im-deployable-namespace.fullname" . }}-test-connection"
  labels:
    {{- include "im-deployable-namespace.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "im-deployable-namespace.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
